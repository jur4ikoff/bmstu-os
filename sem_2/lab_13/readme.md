cat /proc/mounts
mount | grep myfs

1. прям говорит вроде покажи ls -ial
ls -ial /mnt
2. показать что смонтированы две ФС (две последние строки)
sudo cat /proc/mounts
3. показать slab (наш первая строка)
sudo cat /proc/slabinfo
4. показать что зарегистрирована ФС (тоже вроде 1 строка)
cat /proc/filesystems

## вопросы устные:
1. какие точки входа? - init, exit, mount, kill (superblock)
2. зачем inode создаем? - точка монтирования
3. какие минимальные дейсвия позволяют свою ФС зарегистрировать? (не монтировать, а зарегистрировать, какая функция) - register file system
4. какие поля в структуре обязательно указать? - this module и имя
5. а для монтирования что нужно? - надо написать свою функцию mount и зарегистрировать ее через file system type
6. 16 байт 256 раз записали, на 257 раз создаст новую страницу
7. Что передаётся на вход функции register_filesystem - указатель на структуру file_system_type неправильный