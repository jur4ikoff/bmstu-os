1. рассказать где мы вешаемся на линию irq, рассказать обо всех параметрах этой функции (request_irq)
2. как мы освобождаем линию? (вызываем в командной строке команду rmmod)

и показать вывод
cat /proc/interrupts и ps ajx, там найти kworker для workqueue

И показать что в /proc/interrupts висят два обработчика прерывания на первой линии. Если что, там где написана цифра 1 - там будут в конце той строки через запятую перечислены обработчики прерываний

Поток ядра называется как-то типо [kworker/R-irq_w]

Там ещё могут быть [kworker/XX:Y-irq_w], где XX --- номер ядра, Y --- номер воркера

Если что, irq_w это обрезает сам ps, на самом деле это irq_workqueue
